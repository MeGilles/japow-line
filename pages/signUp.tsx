import React from "react";
import { Layout } from "../components"

export default function SignUp() {
    return (
        <Layout>
            <div className="default_centered_div">
                This is a page dedicated to SignUp !
            </div>
        </Layout>
    );
}